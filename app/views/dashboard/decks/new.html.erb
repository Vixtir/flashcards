<%= simple_form_for @deck, url: dashboard_decks_path do |f| %>
   <%= f.input :title, label: t('decks.new.title'), placeholder: "Животные" %>
   <%= f.button :submit, t('decks.new.button') %> 
<% end %>

